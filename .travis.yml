language: python
python:
- "3.6"

git:
  depth: 1

install:
  - pip install mkdocs mkdocs-material

script:
  - mkdocs build --verbose --clean --strict

deploy:
  provider: pages
  skip_cleanup: true
  github_token: $github_token
  local_dir: docs
  on:
    branch: master
